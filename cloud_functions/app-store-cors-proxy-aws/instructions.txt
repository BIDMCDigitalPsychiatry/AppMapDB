Note: Ensure npm is the same version as the target version configured for the lambda

1. Run npm install to generate the node_modules directory.
2. Zip contents of folder and upload to AWS lambda console.
